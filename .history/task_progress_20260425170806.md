# 性能优化任务清单

## 已完成

- [x] 分析账单明细页面卡顿原因
- [x] 安装 `@tanstack/react-virtual` 虚拟滚动库
- [x] **Details.jsx**: 实现虚拟滚动，DOM 节点量从「几百行」→「可视区域 ~30 行」
- [x] **Details.jsx**: 搜索输入 120ms 防抖，避免每次按键触发筛选计算
- [x] **Details.jsx**: TxRow 组件包裹 `React.memo`，消除无条件重渲染
- [x] **index.jsx**: Toast 状态分离到独立 `ToastProvider` Context，避免 toast 变化级联刷新全局
- [x] **useFinanceData.js**: 移除内部 toast 管理，改为从外部注入 `showToast`
- [x] **finance.css**: 新增虚拟表格布局 CSS (`.virt-table-*`)
- [x] **finance.css**: 保留原 `.table` 样式兼容性（虚拟表格不依赖 `<table>`）
- [x] **Sidebar CSS 动画优化**: `width` → `transform translateX`，GPU 硬件加速
- [x] 验证编译无报错

## 验证方法

```bash
cd lifeos-react && npx vite build
# 输出：✓ built in 3.97s，125 modules transformed，无报错
```
